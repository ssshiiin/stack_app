<div class="wrapper">
  <h1 class="wrapper__title">日報一覧</h1>
  <ul>
    <% @users.each do |user| %>
    <li class="list__item">
      <%= link_to user.name, "/users/#{user.id}" %>
      <span><%= user.posts_count %>日目</span>
    </li>
    <% end %>
  </ul>
</div>
